<% if @question.errors.present? %>
<% else %>
$(".questionDetails").html("<%= escape_javascript render(partial:  'surveys/question_details', locals: {survey: @survey, survey_questions: @survey_questions }) %>");
$(".close-popup").click();
$(".ajax-loader").hide();
<% end %>